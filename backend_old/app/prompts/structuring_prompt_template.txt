Analyze the following raw user input and structure it into a JSON object.
The JSON object should only contain keys corresponding to the sections provided in the "Template Sections" list.
For each section:
- If the content for that section consists of multiple distinct points, list items, or enumerated entries, return it as a JSON array of strings.
- Otherwise, return it as a single JSON string.
- If a section is not present in the raw text, omit the key from the JSON object or set its value to null.

Template Sections:
[{template_sections}]

Raw User Input:
---
{raw_text}
---

JSON Output: